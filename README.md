# UPM-langchainTest
Demo for presentation about Langchain and LangGraph

## Quick Start

To get started with this project, follow these steps:

1. Create a virtual environment:
   ```bash
   python -m venv .venv
   ```

2. Activate the virtual environment:
   ```bash
   source .venv/bin/activate
   ```

3. Install the required dependencies:
   ```bash
   pip install -r requirements.txt
   ```

4. Copy the `.env.template` file to `.env` and add your Gemini API key:
   ```bash
   cp .env.template .env
   ```
   Then, open `.env` and replace `YOUR_GEMINI_API_KEY` with your actual key.

5. Run the Jupyter notebooks:
   - For a demo of Langchain, open and run the `langchain-demo.ipynb` notebook in your IDE.
